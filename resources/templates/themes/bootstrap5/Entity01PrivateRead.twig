{% extends scShortPath(layout ?? 'layout_admin.twig') %}

{% block title %}{{ options.title }} - {{ entity01.title }}{% endblock %}

{% block content_toolbar %}
    {% if scHasAccess('AdminModule01Index') %}
        <a href="{{ scLink('AdminModule01Index') }}" class="btn btn-sm btn-outline-secondary">Список</a> 
    {% endif %}
    {% if scHasAccess('AdminModule01Edit') %}
        <a href="{{ scLink('AdminModule01Edit', {id: entity01.id}) }}" class="btn btn-sm btn-outline-secondary">Редактировать</a>
    {% endif %}
    {% if scHasAccess('AdminModule01Delete') %}
        <a class="setcms-action-btn btn btn-sm btn-outline-secondary" 
           setcms-method="POST" 
           setcms-action="{{ scLink('AdminModule01Delete', {id: entity01.id}) }}"  
           setcms-redirect="{{ scLink('AdminModule01Index') }}">
            Удалить
        </a>
    {% endif %}
{% endblock %}

{% block content %}
    <table class="table">
        <tr>
            <td>field01</td><td>{{ entity01.field01 }}</td>
        </tr>
    </table>
{% endblock %}