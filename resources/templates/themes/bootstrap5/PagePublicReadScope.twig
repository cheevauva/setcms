{% extends scPath('layout.twig') %}

{% block content_title %}{{ page.title }}{% endblock %}

{% block content %}
    <h1>{{ page.title }}</h1>
    <p>
        {{ scMarkdown(page.content)|raw }}
    </p>
{% endblock %}