{% extends setcms.path('layout.twig') %}

{% block content_title %}{{ page.title }}{% endblock %}

{% block content %}
    <h1>{{ page.title }}</h1>
    <p>
        {{ setcms.markdown(page.content)|raw }}
    </p>
{% endblock %}