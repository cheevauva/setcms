{% macro checkbox(params) %}
    <input name="{{ params.name }}" class="form-check-input" type="checkbox" value="{{ params.value }}" {% if params.disabled %}disabled="disabled"{% endif %}>
{% endmacro %}
