{% extends theme_path('layout.twig') %}

{% block content %}
    {% for entity in entities %}
        <article class="blog-post">
            <h2 class="blog-post-title">{{ entity.title }}</h2>
            <p class="blog-post-meta">{{ entity.dateCreated.format('d.m.Y H:i:s') }} by <a href="#">Admin</a></p>
            <p>
                {{ setcms_markdown(entity.message) }}
                <a href="{{ link('post_read_by_slug', {slug: entity.slug}) }}">Читать</a>
                {% if is_admin() %}
                    | <a href="{{ link('action_record_admin', {module: entity.module, action: 'saveform', id: entity.id}) }}">Редактировать</a>
                {% endif %}
            </p>
        </article>

    {%  endfor %}
    {% if is_admin() %}
        <a href="{{ link('action_admin', {module: currentModule, action: 'saveform'}) }}">Создать запись</a>
    {% endif %}
{% endblock %}
